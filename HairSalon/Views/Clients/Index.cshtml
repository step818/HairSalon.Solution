@using HairSalon.Models;

<h4>All Clients</h4>
@if(Model.Count == 0)
{
  <p>There are no clients to display.</p>
}

@foreach (Client client in @Client.GetAll())
{
  <a href="/stylists/@Stylist.Find(@client.GetStylistId()).GetId()/clients/@client.GetId()">@client.GetClientName()</a><br>
}
